<%- include('./partials/header') %>

<table id="list">
  <thead>
    <tr>
      <th>Airline</th>
      <th>Airport</th> 
      <th>Flight </th>
  </thead>
  <tbody>
    <% flights.forEach(function(f) { %>
      <tr>
         <td><%= f.airline %></td>
         <td><%= f.airport %></td>
        <td><a href="./flights/<%= f._id %>">DETAILS</a></td>
        <td>
          <% const dt=f.Departure; const departsDate=dt.toISOString().slice(0, 16).replace("T", " " ); %>
            <%= Departure %>
        </td>
        </tr>
        <% }); %>
  </tbody>
</table>


<%- include('./partials/footer') %>